# =============================================================================
# ELEPHMIND - ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# 🔒 SECURITY: Never commit .env file to version control
# Copy this file to .env and fill in your secure values

# ============================================
# ENVIRONMENT
# ============================================
# Options: development | staging | production
ENVIRONMENT=development

# ============================================
# SECURITY KEYS (🔴 REQUIRED IN PRODUCTION)
# ============================================

# JWT Secret for authentication tokens
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your_jwt_secret_key_here_64_chars_minimum

# Encryption key for sensitive data (Fernet)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_encryption_key_here

# ============================================
# DATABASE
# ============================================
# PostgreSQL (recommended for production)
DATABASE_URL=postgresql://user:password@localhost:5432/elephmind

# SQLite (development only)
# DATABASE_URL=sqlite:///./elephmind.db

# ============================================
# SERVER CONFIGURATION
# ============================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8022
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,https://elephmind.ai

# Maximum concurrent users
MAX_CONCURRENT_USERS=100

# ============================================
# STORAGE (Medical Images)
# ============================================
# Storage mode: LOCAL | OPENSTACK
STORAGE_MODE=LOCAL

# OpenStack Swift (if STORAGE_MODE=OPENSTACK)
# OS_AUTH_URL=https://auth.cloud.ovh.net/v3
# OS_USERNAME=your_username
# OS_PASSWORD=your_password
# OS_PROJECT_NAME=your_project
# OS_REGION_NAME=GRA

# ============================================
# FRONTEND (React/Vite)
# ============================================
# Backend API URL (for frontend .env)
VITE_API_URL=http://127.0.0.1:8022

# ============================================
# OPTIONAL SERVICES
# ============================================
# Qdrant Vector Database
QDRANT_URL=http://localhost:6333

# Monitoring
ENABLE_METRICS=true
# SENTRY_DSN=https://your_sentry_dsn_here

# ============================================
# 🔒 SECURITY NOTES
# ============================================
# ✅ Use different secrets for dev/staging/prod
# ✅ Rotate secrets every 90 days
# ✅ Store production secrets in:
#    - AWS Secrets Manager
#    - GCP Secret Manager
#    - Azure Key Vault
#    - HashiCorp Vault
# ✅ Never share .env file or commit to Git
